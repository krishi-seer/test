# Architecture

> Auto-generated by /map on 2026-04-14

## Overview

Krishi-Seer is a Next.js 14 web application designed as an agricultural platform. It integrates with Supabase for the database, and has numerous API routes reflecting integrations with AI models (HuggingFace, document extraction, chat), schemes synchronization, and alerts.

```text
┌─────────────────────────────────────────┐
│          [Next.js App Router]           │
├─────────────────────────────────────────┤
│    [API Routes / Feature Modules]       │
├─────────────────────────────────────────┤
│          [Supabase Database]            │
└─────────────────────────────────────────┘
```

## Components

### API Layer (`app/api`)
- **Purpose:** Handles external integrations, data synchronization, and ML functionalities (e.g., `chat-stream`, `extract-soil-card`, `hf`, `plantid`, `sync-schemes`, `alerts/broadcast`).
- **Location:** `app/api/`
- **Dependencies:** Supabase, external AI APIs (like HuggingFace and possibly OpenAI).

### Document Extractor (`app/features/document-extractor`)
- **Purpose:** Handling document text or data extraction.
- **Location:** `app/features/document-extractor/`

### UI Components (`components/ui`)
- **Purpose:** Shared reusable styled components (likely Headless UI / Radix).
- **Location:** `components/ui/`
- **Dependencies:** `@headlessui/react`, `@radix-ui/react-slot`, `tailwindcss`.

### i18n Localization Layer
- **Purpose:** Multi-language support (English, Hindi, Odia).
- **Location:** `public/locales/` & setup in `i18n.ts` / `next-i18next`.
- **Dependencies:** `react-i18next`, `i18next`.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Supabase | Database/Auth | Core database and likely authentication (`@supabase/supabase-js`) |
| HuggingFace | API | ML models inference (`app/api/hf`) |
| PlantID | API | Likely identifying plant diseases/types (`app/api/plantid`) |

## Technical Debt

- [ ] Need to verify test coverage.
- [ ] Next.js version says 14.1.0 in package.json, although user mentioned 15 earlier.

## Conventions

**Structure:** Next.js App Router structure with localized assets in `public/locales/`.
**Styling:** Tailwind CSS (`tailwind-merge`).
